apiVersion: v1
kind: Service
metadata:
  name: shouit
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "shouit"
    port: 8080
  selector:
    app: shouit
