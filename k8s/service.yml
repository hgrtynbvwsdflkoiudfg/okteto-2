apiVersion: v1
kind: Service
metadata:
  name: hoution
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hoution"
    port: 8080
  selector:
    app: hoution
