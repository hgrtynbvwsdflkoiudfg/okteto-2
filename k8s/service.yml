apiVersion: v1
kind: Service
metadata:
  name: yerctvio
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yerctvio"
    port: 8080
  selector:
    app: yerctvio
