apiVersion: v1
kind: Service
metadata:
  name: upgreat
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "upgreat"
    port: 8080
  selector:
    app: upgreat
